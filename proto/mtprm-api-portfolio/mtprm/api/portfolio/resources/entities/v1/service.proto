syntax = "proto3";

// buf:lint:ignore PACKAGE_LOWER_SNAKE_CASE
package mtprm.api.portfolio.resources.entities.v1;

import "mtprm/core/entity/v2/message.proto";
import "mtprm/core/id/v1/message.proto";

service Service {
  // Read: get details of a monitored entity
  rpc Read(ReadRequest) returns (ReadResponse) {}
  // List: retrieve a list of all monitored entities; optionally pass a grade to get a filtered list of entities with specified grade
  rpc List(ListRequest) returns (stream ListResponse) {}
}

message ReadRequest {
  mtprm.core.id.v1.PrefixedId entity_id = 1;
}

message ReadResponse {
  mtprm.core.entity.v2.Entity entity = 1;
}

message ListRequest {
  optional Grade grade = 1;
}

message ListResponse {
  repeated mtprm.core.entity.v2.Entity entities = 1;
}

enum Grade {
  GRADE_UNSPECIFIED = 0;
  GRADE_A = 1;
  GRADE_B = 2;
  GRADE_C = 3;
  GRADE_D = 4;
  GRADE_F = 5;
}
